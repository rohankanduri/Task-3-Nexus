<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <title>Doctor</title>
    <style>
        body {
            background-image: url('https://media.istockphoto.com/id/1300505874/photo/diagnostic-tools-get-a-digital-upgrade.jpg?s=612x612&w=0&k=20&c=GTCOQrIL9atqBToWWnyVp55UOLr2DGkwd1ct0OSjPuw=');
            background-size: cover;
            background-repeat: no-repeat;
            background-position: center;
            color: white;
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            backdrop-filter: blur(5px); /* Add backdrop filter for blur effect */
        }

        .container {
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
        }

        .header {
            text-align: center;
            margin-bottom: 20px;
        }

        .card {
            background-color: rgb(0, 0, 0, 0.65);
            border-radius: 20px;
            padding: 20px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.3);
            width: 400px;
            max-width: 90%;
            margin-bottom: 20px;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }

        .card:hover {
            background-color: rgba(255, 255, 255, 0.5);
        }

        .card h3 {
            margin-bottom: 10px;
        }

        .card p {
            margin: 0;
        }

        @media (max-width: 768px) {
            .card {
                width: 100%;
            }
        }
    </style>
</head>

<body>
    <div class="container">
        <h1 class="header">MY APPOINTMENTS</h1>
        <% for (let doctor of doctors) { %>
        <div class="card" onclick="window.location.href = '/doctors/<%=doctor._id%>';">
            <h3><%= doctor.fname %></h3>
            <p><%= doctor.description %></p>
        </div>
        <% } %>
    </div>
</body>

</html>
